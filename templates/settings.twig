{% import "_includes/forms" as forms %}

{{ forms.editableTableField({
    label: 'Enabled Element Types'|t,
    id: 'elementSearchElementTypes',
    name: 'elementSearchElementTypes',
    instructions: 'You are able to enable or disable certain Element Types from the direct element search when the palette is opened.'|t,
    cols: {
        elementType: {
            heading: 'Element Type'|t,
            type: 'singleline'
        },
        enabled: {
            heading: 'Enable (1) / Disable (0)'|t,
            type: 'number'
        },
    },
    rows: elementSearchElementTypes,
    first: true
}) }}
